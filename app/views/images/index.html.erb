<div class="pic">
<div class="backpic">
</div>
  
<p id="notice"><%= notice %></p>
<div class="pic">
  <div class="backpic">
  </div>

</div>
<h1>Schedules</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Caption</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @images.each do |image| %>
      <tr>
        <td><%= image.title %></td>
        <td><%= image.caption %></td>
        <td><%= link_to 'Show', image %></td>
        <td><%= link_to 'Edit', edit_image_path(image) %></td>
        <td><%= link_to 'Destroy', image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add schedules', new_image_path %><br>
<%= link_to 'Home', welcome_index_path %>
